{
  "name": "mob3",
  "version": "0.1.0",
  "description": "ECS application architecture for Three.js — without replacing Three.js",
  "type": "module",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "src"
  ],
  "sideEffects": false,
  "scripts": {
    "build": "tsc -p tsconfig.json && mkdir -p dist/parallel dist/abi && cp src/parallel/*.mjs dist/parallel/ && cp src/abi/*.mjs dist/abi/ && mkdir -p dist/../tests/fixtures/wasm 2>/dev/null; true",
    "compile:wat": "node scripts/compile-wat.mjs",
    "clean": "rm -rf dist"
  },
  "keywords": [
    "ecs",
    "entity-component-system",
    "three.js",
    "game",
    "typescript"
  ],
  "license": "MIT"
}
